import React from "react";
import LocalMallOutlinedIcon from "@mui/icons-material/LocalMallOutlined";

const CartIcon: React.FC = () => {
  return <LocalMallOutlinedIcon sx={{ fontSize: 24, color: "black" }} />;
};

export default CartIcon;
